{
  "version": 2,
  "buildCommand": "pip install -r requirements.txt && python manage.py check && python manage.py collectstatic --noinput || true && python manage.py migrate --noinput || true",
  "routes": [
    { "src": "/static/(.*)", "dest": "/static/$1" },
    { "src": "/(.*)", "dest": "myportfolio_project/wsgi.py" }
  ]
}
